import { RoutePlaceholder } from "@/app/_components/route-placeholder";

export default function FavoritesPage() {
  return (
    <RoutePlaceholder
      title="찜한 촬영자 화면 준비 중입니다"
      description="찜 목록 조회는 이후 단계에서 구현됩니다."
    />
  );
}
