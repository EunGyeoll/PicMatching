import { RoutePlaceholder } from "@/app/_components/route-placeholder";

export default function PhotographerAvailabilityPage() {
  return (
    <RoutePlaceholder
      title="예약 가능 일정 관리 화면 준비 중입니다"
      description="요일별 운영 시간 및 차단 일정 등록은 이후 단계에서 구현됩니다."
    />
  );
}
